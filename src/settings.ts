export interface RememberCodeBlockLangSettings {
    lastUsedLanguage: string;
    defaultLanguage: string;
    autoInjectEnabled: boolean;
}

export const DEFAULT_SETTINGS: RememberCodeBlockLangSettings = {
    lastUsedLanguage: '',
    defaultLanguage: '',
    autoInjectEnabled: true
};



